name: Clean
on: 
  workflow_call: 

jobs:
  Clean:
    runs-on: self-hosted-1
    steps:
      - name: Check out repository code
        uses: actions/checkout@v2
        with:
          ref: ${{ github.ref }}
      - name: Done
        run: make clean